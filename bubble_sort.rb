# def bubble_sort(array)
  
# end

#element comparison test:
def element_comparison(array)
  if array[0] > array[1]
    value1 = array[0]
    value2 = array[1]
    array[0] = value2
    array[1] = value1
  end
  array
end

# Tests for first iteration loop
# So 1 loop through a big array
def array_sorter(array)
  array.each_with_index do |element, index|
    
  end
end